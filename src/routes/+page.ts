// Static profile page: prerender to a fully-formed HTML document so crawlers
// and social scrapers get complete markup (title, meta, JSON-LD) with zero JS,
// and visitors get an instant first paint.
export const prerender = true;
